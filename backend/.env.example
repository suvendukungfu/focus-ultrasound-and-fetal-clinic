# ══════════════════════════════════════════════════════════════
# Focus Ultrasound & Fetal Clinic — Environment Configuration
# ══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values.
# Required keys are marked with [REQUIRED], optional with [OPTIONAL].

# ─── Server ───────────────────────────────────────────────────
PORT=4000                                    # [REQUIRED] Express server port
NODE_ENV=development                         # [REQUIRED] development | production
REGION=GLOBAL-1                              # [OPTIONAL] Multi-region identifier

# ─── Database ─────────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@localhost:5432/focus_clinic?schema=public"  # [REQUIRED]

# ─── Redis (Caching & Queues) ─────────────────────────────────
REDIS_URL="redis://localhost:6379"           # [OPTIONAL] Redis connection string

# ─── Authentication (JWT) ────────────────────────────────────
JWT_SECRET="your-secure-jwt-secret-key"      # [REQUIRED] Minimum 32 characters
JWT_REFRESH_SECRET="your-refresh-secret"     # [REQUIRED] Minimum 32 characters
JWT_EXPIRES_IN="1h"                          # [OPTIONAL] Access token TTL
JWT_REFRESH_EXPIRES_IN="7d"                  # [OPTIONAL] Refresh token TTL

# ─── OpenAI (AI Services) ────────────────────────────────────
OPENAI_API_KEY=""                            # [OPTIONAL] Enables GPT-4o for blog drafts, meta descriptions, keywords, lead replies
                                             # Leave empty for intelligent template/heuristic fallbacks

# ─── WhatsApp Cloud API ──────────────────────────────────────
WHATSAPP_VERIFY_TOKEN="focus_clinic_verify"  # [REQUIRED for WhatsApp] Webhook verification token
WHATSAPP_PHONE_NUMBER_ID=""                  # [REQUIRED for WhatsApp] Meta Business phone number ID
WHATSAPP_ACCESS_TOKEN=""                     # [REQUIRED for WhatsApp] Permanent access token from Meta developer portal

# ─── Google Integrations ─────────────────────────────────────
GOOGLE_PLACES_API_KEY=""                     # [OPTIONAL] For fetching Google Reviews
GOOGLE_PLACE_ID=""                           # [OPTIONAL] Google Maps Place ID for the clinic
GOOGLE_MAPS_API_KEY=""                       # [OPTIONAL] For Maps embed
NEXT_PUBLIC_GA_ID=""                         # [OPTIONAL] Google Analytics 4 Measurement ID (G-XXXXXXXXXX)
GOOGLE_ANALYTICS_ID=""                       # [OPTIONAL] Server-side GA fallback

# ─── Next.js Frontend ────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"   # [REQUIRED] Public-facing base URL (no trailing slash)
NEXT_PUBLIC_API_URL="http://localhost:4000/api/v1"  # [REQUIRED] Backend API base URL

# ─── Email (SMTP) ────────────────────────────────────────────
SMTP_HOST=""                                 # [OPTIONAL] e.g. smtp.gmail.com
SMTP_PORT=587                                # [OPTIONAL] 587 for TLS, 465 for SSL
SMTP_USER=""                                 # [OPTIONAL] Email address
SMTP_PASS=""                                 # [OPTIONAL] App-specific password

# ─── Logging ─────────────────────────────────────────────────
LOG_LEVEL="info"                             # [OPTIONAL] error | warn | info | http | debug
